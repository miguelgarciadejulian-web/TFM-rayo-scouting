# =============================================================================
#  Rayo Vallecano Scouting Tool — dependencias
#  Probado con Python 3.10–3.12. Instala con:  pip install -r requirements.txt
# =============================================================================

# ── Núcleo de datos ──────────────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # lectura/escritura de los .parquet de datos
pyyaml>=6.0            # carga de la configuración (config/*.yaml)
unidecode>=1.3         # búsqueda tolerante a tildes

# ── Dashboard (Dash / Plotly) ─────────────────────────────────────────────────
dash[diskcache]>=2.17  # incluye diskcache para los background callbacks (PDF sin timeout)
dash-bootstrap-components>=1.6
plotly>=5.22

# ── Análisis / similitud ──────────────────────────────────────────────────────
scikit-learn>=1.4      # StandardScaler, cosine_similarity (comparador)
scipy>=1.13

# ── Gráficos para los informes ────────────────────────────────────────────────
matplotlib>=3.8        # radares y gauges embebidos en los PDF (backend Agg)

# ── Generación de PDF ─────────────────────────────────────────────────────────
reportlab>=4.1
xhtml2pdf>=0.2.11      # motor PDF en Windows (puro Python, sin dependencias de sistema)
Pillow>=10.0           # tratamiento de imágenes para los informes

# ── Enriquecido de datos (fotos / valores de mercado) ──────────────────────────
requests>=2.32
beautifulsoup4>=4.12

# =============================================================================
#  OPCIONALES
# =============================================================================
# WeasyPrint produce PDF de mayor calidad, pero requiere GTK3 y NO se usa en
# Windows (la app cae automáticamente a xhtml2pdf). Descomenta sólo en Linux/Mac.
# weasyprint>=60.0
#
# Sólo para regenerar datos con los scripts de scripts/ (ETL Transfermarkt):
# cloudscraper>=1.2     # esquiva Cloudflare al scrapear Transfermarkt
# tqdm>=4.66            # barras de progreso en los procesos batch
#
# Sólo para ejecutar los tests:
# pytest>=8.2
